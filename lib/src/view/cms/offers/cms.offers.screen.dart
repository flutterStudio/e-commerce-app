import 'package:flutter/cupertino.dart';

class CMSOffersScreen extends StatelessWidget {
  const CMSOffersScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Offers screen");
  }
}
